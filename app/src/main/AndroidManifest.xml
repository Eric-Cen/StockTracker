<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.mcarving.stocktracker">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".portfolios.PortfoliosActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".portfolioDetail.PortfolioActivity">
        </activity>
        <activity android:name=".stockDetail.StockDetailActivity">
        </activity>
        <activity android:name=".SettingsActivity">
        </activity>
        <activity android:name=".addStock.StockAddActivity">
        </activity>
        <activity android:name=".alerts.AlertListActivity">
        </activity>
        <activity android:name=".addAlert.AlertAddActivity">
        </activity>
    </application>

</manifest>